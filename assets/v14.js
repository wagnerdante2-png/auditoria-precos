(()=>{"use strict";
const D=window.AUDITORIA_DATA,T=D.metadata.targetLabelsPerStore||6000,$=id=>document.getElementById(id);
const rec=new Map(D.records.map(r=>[`${r.storeId}|${r.month}`,r])),stores=new Map(D.stores.map(s=>[s.id,s]));
const nf=new Intl.NumberFormat('pt-BR'),cf=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}),pf=new Intl.NumberFormat('pt-BR',{minimumFractionDigits:1,maximumFractionDigits:1});
const monthFmt=new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric',timeZone:'UTC'});
const state={view:'dashboard',month:D.metadata.latestMonth,scope:'network',id:'',metric:'labels',q:'',storeRegion:'ALL',storeStatus:'ALL',dbRegion:'ALL',dbStatus:'ALL'};
const titles={dashboard:['Visão Executiva','Síntese executiva para reunião e tomada de decisão.'],comparison:['Análise Comparativa','Comparações temporais e benchmark do período selecionado.'],stores:['Lojas','Leitura operacional detalhada por unidade.'],regions:['Regionais','Consolidação gerencial por regional.'],evolution:['Evolução Mensal','Série histórica e outras óticas dos principais indicadores.'],recommendations:['Recomendações','Orientações diretas para o período selecionado.'],database:['Base de Dados','Leitura analítica, semáforos e tendências por loja/']};
const metrics={labels:{label:'Etiquetas auditadas',type:'integer',dir:'up',color:'#5aa99e'},divergences:{label:'Divergências encontradas',type:'integer',dir:'down',color:'#dd6b7f'},noPrice:{label:'Produtos sem preço',type:'integer',dir:'down',color:'#d4a65b'},discountCount:{label:'Quantidade de descontos',type:'integer',dir:'down',color:'#897eb8'},discountValue:{label:'Valor de descontos',type:'currency',dir:'down',color:'#668bd5'}};
const regionColors={'GUARDIÕES DA CHAMA':'#b97943','GUARDIÕES DA LUZ':'#5aa99e','OCEANO MARA':'#668bd5','RAIO BRAVO':'#c29d52','RAÍZES DO LAR':'#72a65e','VENTO DOURADO':'#897eb8'};
const safe=v=>v==null?'':String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const attr=v=>safe(v).replace(/`/g,'&#96;');
const fmt=(v,t='integer')=>v==null||Number.isNaN(v)?'—':t==='currency'?cf.format(v):t==='percent'?pf.format(v)+'%':t==='decimal'?pf.format(v):nf.format(Math.round(v));
const mt=m=>{if(!m)return'—';let s=monthFmt.format(new Date(`${m}-01T00:00:00Z`));return s.charAt(0).toUpperCase()+s.slice(1)};
const sum=a=>a.reduce((x,y)=>x+(Number.isFinite(y)?y:0),0),avg=a=>{const b=a.filter(Number.isFinite);return b.length?sum(b)/b.length:null},get=(id,m=state.month)=>rec.get(`${id}|${m}`)||null;
const shiftMonth=(m,d)=>{const[y,mo]=m.split('-').map(Number),x=new Date(Date.UTC(y,mo-1+d,1));return`${x.getUTCFullYear()}-${String(x.getUTCMonth()+1).padStart(2,'0')}`},prevMonth=m=>shiftMonth(m,-1),yearAgo=m=>shiftMonth(m,-12),pct=(a,b)=>a==null||b==null||b===0?null:(a-b)/Math.abs(b)*100;
function scopeStores(scope=state.scope,id=state.id){if(scope==='region')return D.stores.filter(s=>s.region===id);if(scope==='store')return D.stores.filter(s=>String(s.id)===String(id));return D.stores}
function summary(m=state.month,scope=state.scope,id=state.id){const ss=scopeStores(scope,id),rr=ss.map(s=>get(s.id,m)).filter(Boolean),op=rr.filter(r=>r.labels!=null),labels=sum(rr.map(r=>r.labels)),div=sum(rr.map(r=>r.divergences)),np=sum(rr.map(r=>r.noPrice)),dc=sum(rr.map(r=>r.discountCount)),dv=sum(rr.map(r=>r.discountValue)),target=op.length*T,hit=op.filter(r=>r.labels>=T).length;return{ss,rr,op,labels,div,np,dc,dv,target,hit,att:target?labels/target*100:null,hitRate:op.length?hit/op.length*100:null,dr:labels?div/labels*1000:null,npr:labels?np/labels*1000:null}}
function currentName(){if(state.scope==='region')return state.id||'Regional';if(state.scope==='store'){const s=stores.get(+state.id);return s?`${s.code} · ${s.name}`:'Loja'}return'Total Rede'}
function networkBench(m=state.month){const rows=D.stores.map(s=>get(s.id,m)).filter(r=>r&&r.labels!=null);return{discountCount:avg(rows.map(r=>r.discountCount)),discountValue:avg(rows.map(r=>r.discountValue)),labels:avg(rows.map(r=>r.labels)),divRate:avg(rows.filter(r=>r.labels).map(r=>(r.divergences||0)/r.labels*1000)),npRate:avg(rows.filter(r=>r.labels).map(r=>(r.noPrice||0)/r.labels*1000))}}
function storeStatus(store,m=state.month){const r=get(store.id,m);if(!r||r.labels==null)return{cls:'neutral',label:'Sem base',reasons:['Sem registro de etiquetas na competência selecionada.']};const b=networkBench(m),reasons=[];if(r.labels<T)reasons.push(`Etiquetas abaixo da meta: ${fmt(r.labels)} de ${fmt(T)}.`);if(r.discountCount!=null&&b.discountCount!=null&&r.discountCount>b.discountCount)reasons.push(`Quantidade de descontos acima da média da rede: ${fmt(r.discountCount)} vs ${fmt(b.discountCount,'decimal')}.`);if(r.discountValue!=null&&b.discountValue!=null&&r.discountValue>b.discountValue)reasons.push(`Valor de descontos acima da média da rede: ${fmt(r.discountValue,'currency')} vs ${fmt(b.discountValue,'currency')}.`);if(reasons.length)return{cls:'bad',label:'Crítico',reasons};const attention=[];if(r.discountCount!=null&&b.discountCount!=null&&r.discountCount>=b.discountCount*.8)attention.push(`Quantidade de descontos em ${fmt(r.discountCount/b.discountCount*100,'percent')} da média da rede.`);if(r.discountValue!=null&&b.discountValue!=null&&r.discountValue>=b.discountValue*.8)attention.push(`Valor de descontos em ${fmt(r.discountValue/b.discountValue*100,'percent')} da média da rede.`);return attention.length?{cls:'warn',label:'Atenção',reasons:attention}:{cls:'good',label:'Saudável',reasons:[`Meta atingida (${fmt(r.labels)} etiquetas) e descontos abaixo de 80% da média da rede.`]}}
function trend(cur,prev,dir,label,type='integer'){const d=pct(cur,prev);if(d==null)return`<span class="trend neutral has-tip" data-tip="Sem base comparável no mês anterior.">• sem base</span>`;const good=dir==='up'?d>=0:d<=0,cls=d===0?'neutral':good?'good':'bad',arrow=d>0?'▲':d<0?'▼':'•',tip=`${label}: ${fmt(prev,type)} em ${mt(prevMonth(state.month))} → ${fmt(cur,type)} em ${mt(state.month)}. ${good?'Movimento favorável':'Movimento desfavorável'} para o indicador.`;return`<span class="trend ${cls} has-tip" data-tip="${attr(tip)}">${arrow} ${fmt(Math.abs(d),'percent')}</span>`}
function hero(title,desc){const s=summary(),pm=summary(prevMonth(state.month),state.scope,state.id),d=pct(s.att,pm.att);return`<div class="hero"><div class="hero-top"><div><span class="eyebrow">AUDITORIA DE PREÇOS • ${safe(mt(state.month).toUpperCase())}</span><h2>${safe(title)}</h2><p>${safe(desc)}</p></div><div class="hero-badges"><span class="pill live">● ${s.op.length} lojas com dados operacionais</span><span class="pill">M-1: ${fmt(pm.att,'percent')}</span></div></div><div class="hero-stats"><div class="hero-stat"><span>Escopo</span><strong>${safe(currentName())}</strong></div><div class="hero-stat"><span>Atingimento</span><strong>${fmt(s.att,'percent')}</strong></div><div class="hero-stat"><span>Δ mês anterior</span><strong>${d==null?'—':`${d>=0?'▲':'▼'} ${fmt(Math.abs(d),'percent')}`}</strong></div><div class="hero-stat"><span>Lojas na meta</span><strong>${s.hit}/${s.op.length}</strong></div></div></div>`}
function metricCard(label,value,sub,color='#668bd5'){return`<article class="metric-card" style="--accent:${color}"><span class="label">${safe(label)}</span><strong>${value}</strong><small>${sub}</small></article>`}
function scopeStatus(){if(state.scope==='store'){const s=stores.get(+state.id);return s?storeStatus(s):{cls:'neutral',label:'Sem base',reasons:['Sem loja selecionada.']}}const ss=scopeStores(),stats=ss.map(s=>storeStatus(s)),bad=stats.filter(x=>x.cls==='bad').length,warn=stats.filter(x=>x.cls==='warn').length;return bad?{cls:'bad',label:'Clítico',reasons:[a${bad} unidade(s) críticas no escopo atual.`]}:warn?{cls:'warn',label:'Atenção',reasons:[a${warn} unidade(s) em atenção no escopo atual.`]}:{cls:'good',label:'Saudável',reasons:['Nenhuma unidade crítica no escopo atual.']}}
function renderDashboard(){const s=summary(),pm=summary(prevMonth(state.month),state.scope,state.id),status=scopeStatus(),prior=scopeStores().map(x=>({s:x,st:storeStatus(x)})).filter(x=>x.st.cls==='bad').slice(0,6),regs=D.regions.map(r=>[r,summary(state.month,'region',r)]).sort((a,b)=>(b[1].att||0)-(a[1].att||0));$('view-dashboard').innerHTML=hero(currentName(),'Leitura consolidada de cobertura, qualidade e descontos do período.')+`<div class="grid-6">${metricCard('Etiquetas auditadas',fmt(s.labels),`${trend(s.labels,pm.labels,'up','Etiquetas auditadas')} • meta ${fmt(s.target)}`,'#5aa99e')}${metricCard('Atingimento',fmt(s.att,'percent'),`${trend(s.att,pm.att,'up','Atingimento','percent')} • ${s.hit}/${s.op.length} lojas na meta`,'#70bd88')}${metricCard('Divergências / mil',fmt(s.dr,'decimal'),`${trend(s.dr,pm.dr,'down','Divergências por mil','decimal')} • ${fmt(s.div)} ocorrências`,'#dd6b7f')}${metricCard('Sem preço / mil',fmt(s.npr,'decimal'),`${trend(s.npr,pm.npr,'down','Sem preço por mil','decimal')} • ${fmt(s.np)} produtos`,'#d4a65b')}${metricCard('Qtd. descontos',fmt(s.dc),`${trend(s.dc,pm.dc,'down','Quantidade de descontos')} • período atual`,'#897eb8')}${metricCard('Valor descontos',fmt(s.dv,'currency'),`${trend(s.dv,pm.dv,'down','Valor de descontos','currency')} • período atual`,'#668bd5')}</div><div class="two-col"><article class="panel"><div class="panel-head"><div><h3>Cenário em 10 segundos</h3><p>Indicadores essenciais do escopo selecionado.</p></div><span class="semaphore ${status.cls} has-tip" data-tip="${attr(status.reasons.join(' '))}"><i></i>${status.label}</span></div><div class="gauge-wrap"><div class="gauge" style="--p:${Math.min(100,s.att||0)};--gauge:${s.att>=100?'#70bd88':s.att>=85?'#d4a65b':'#dd6b7f'}"><div><strong>${fmt(s.att,'percent')}</strong><span>atingimento</span></div></div><div class="bar-list"><div class="bar-row"><span>Etiquetas</span><div class="track"><i style="width:${Math.min(100,s.att||0)}%;--bar:#5aa99e"></i></div><strong>${fmt(s.labels)}</strong></div><div class="bar-row"><span>Lojas na meta</span><div class="track"><i style="width:${s.hitRate||0}%;--bar:#668bd5"></i></div><strong>${fmt(s.hitRate,'percent')}</strong></div><div class="bar-row"><span>Divergências/mil</span><div class="track"><i style="width:${Math.min(100,(s.dr||0)*6)}%;--bar:#dd6b7f"></i></div><strong>${fmt(s.dr,'decimal')}</strong></div><div class="bar-row"><span>Sem preço/mil</span><div class="track"><i style="width:${Math.min(100,(s.npr||0)*2)}%;--bar:#d4a65b"></i></div><strong>${fmt(s.npr,'decimal')}</strong></div></div></div></article><article class="panel"><div class="panel-head"><div><h3>Prioridades do período</h3><p>Meta de etiquetas e descontos comparados à média da rede.</p></div></div><div class="priority-list">${prior.map(x=>`<div class="priority-item"><div><strong>${x.s.code} · ${safe(x.s.name)}</strong><span>${safe(x.s.region)}</span></div><span class="semaphore bad has-tip" data-tip="${attr(x.st.reasons.join(' '))}"><i></i>Crítico</span></div>`).join('')||'<div class="priority-item">Nenhuma unidade crítica no escopo.</div>'}</div></article></div><div class="two-col"><article class="panel"><div class="panel-head"><div><h3>Benchmark por regional</h3><p>Atingimento na competência selecionada.</p></div></div><div class="bar-list">${regs.map([r,x]=>`<div class="bar-row"><span>${safe(r)}</span><div class="track"><i style="width:${Math.min(100,x.att||0)}%;--bar:${regionColors[r]||'#668bd5'}"></i></div><strong>${fmt(x.att,'percent')}</strong></div>`).join(')}</div></article><article class="panel"><div class="panel-head"><div><h3>Leitura gerencial</h3><p>Pontos de controle para a reunião.</p></div></div><div class="bar-list"><div class="bar-row"><span>Valor descontos</span><div class="track"><i style="width:${Math.min(100,Math.abs(pct(s.dv,pm.dv)||0)*3)}%;--bar:#668bd5"></i></div><strong>${fmt(s.dv,'currency')}</strong></div><div class="bar-row"><span>Qtd. descontos</span><div class="track"><i style="width:${Math.min(100,Math.abs(pct(s.dc,pm.dc)||0)*3)}%;--bar:#897eb8"></i></div><strong>${fmt(s.dc)}</strong></div></div></article></div>`}
function compareBars(label,cur,prev,ya,type,dir){const vals=[cur,prev,ya].filter(v=>v!=null),max=Math.max(1,...vals.map(Math.abs)),rows=[['Atual',mt(state.month),cur,'#668bd5'],['Mês anterior',mt(prevMonth(state.month)),prev','#7d8ca0'],['Ano anterior',mt(yearAgo(state.month)),ya,'#897eb8']];const dm=pct(cur,prev),dy=pct(cur,ya),good=v=>dir==='up'?v>=0:v<=0;return`<article class="compare-card"><h3>${safe(label)}</h3>${rows.map(([n,m,v,c])=>`<div class="compare-line"><span>${n}<br><small>${safe(m)}</small></span><div class="track"><i style="width:${Math.max(2,Math.abs(v||0)/max*100)}%;--bar:${c}"></i></div><strong>${fmt(v,type)}</strong></div>`).join('')}<p style="margin:7px 0 0 ;font-size:8px;color:#8899ac">vs M-1: <span class="trend ${dm==null?neutral':good(dm)?'good':'bad'}">${dm==null?'—':`${dm>0?'▲':'▼'} ${fmt(Math.abs(dm),'percent')}`}</span> • vs AA: <span class="trend ${dy==null?'neutral':good(dy)?'good':'bad'}">${dy==null?'—':`${dy>0?'▲':'▼'} ${fmt(Math.abs(dy),'percent')}`}</span></p></article>`}
function renderComparison(){const cur=summary(),pm=summary(prevMonth(state.month),state.scope,state.id),ya=summary(yearAgo(state.month),state.scope,state.id);let bench=[];if(state.scope==='network'){bench=D.regions.map(r=>({title:r,sub:`${summary(state.month,'region',r).ss.length} lojas`,s:summary(state.month,'region',r)})).sort((a,b)=>(b.s.att||0)-(a.s.att||0))}else if(state.scope==='region'){bench=scopeStores().map(s=>{const r=get(s.id);return{title:`${s.code} · ${s.name}`,sub:s.region,s:{att:r?.labels!=null?r.labels/T*100:null,dr:r?.labels?(r.divergences||0)/r.labels*1000:null,npr:r?.labels?(r.noPrice||0)/r.labels*1000:null}}}).sort((a,b)=>(b.s.att||0)-(a.s.att||0))}else{const st=stores.get(+state.id);bench=[{title:st?.dame||currentName(),sub:'Loja selecionada',s:cur},{title:st?.region||'Regional',sub:'Regional',s:summary(state.month,'region',st?.region)},{title:'Total Rede',sub:'Rede',s:summary(state.month,'network','')}]}const maxAtt=Math.max(100,...bench.map(x=>x.s.att||0));$('view-comparison').innerHTML=hero('Análise Comparativa · '+currentName(),'Comparação clara entre a competência atual, o mês anterior e o mesmo mês do ano anterior.')+`<div class="compare-explain"><div><strong>O que está sendo comparado</strong><p>Atual: ${safe(mt(state.month))} • M-1: ${safe(mt(prevMonth(state.month)))} • Ano anterior: ${safe(mt(yearAgo(state.month)))}. Os valores s�o excibidos na mesma escala de cada indicador.</p></div><div class="legend-swatches"><span><i style="background:#668bd5"></i>Atual</span><span><i style="background:#7d8ca0"></i>M-1</span><span><i style="background:#897eb8"></i>Ano anterior</span></div></div><div class="compare-grid">${compareBars('Etiquetas auditadas',cur.labels,pm.labels,ya.labels,'integer','up')}${compareBars('Atingimento',cur.att,pm.att,ya.att,'percent','up')}${compareBars('Divergências / mil',cur.dr,pm.dr,ya.dr,'decimal','down')}${compareBars('Sem preço / mil',cur.npr,pm.npr,ya.npr,'decimal','down')}${compareBars('Qtd. descontos',cur.dc,pm.dc,ya.dc,'integer','down')}${compareBars('Valor descontos',cur.dv,pm.dv,ya.dv,'currency','down')}</div><div class="two-col"><article class="panel"><div class="panel-head"><div><h3>Benchmark da competência atual</h3><p>${state.scope==='network'?'Regionais comparadas no mesmo mês.':state.scope==='region'?'Lojas da regional comparadas no mesmo mês.':'Loja versus regional e rede.'}</p></div></div><div class="bar-list">${bench.map(x=>`<div class="bar-row"><span>${safe(x.title)}</span><div class="track"><i style="width:${Math.max(2,Math.min(100,(x.s.att||0)/maxAtt*100))}%;--bar:#668bd5"></i></div><strong>${fmt(x.s.att,'percent')}</strong></div>`).join('')}</div></article><article class="panel"><div class="panel-head"><div><h3>Leitura descontos</h3><p>Escopo atual versus rede.</p></div></div><div class="bar-list"><div class="bar-row"><span>Qtd. descontos</span><div class="track"><i style="width:100%;--bar:#897eb8"></i></div><strong>${fmt(cur.dc)}</strong></div><div class="bar-row"><span>Valor descontos</span><div class="track"><i style="width:100%;--bar:#668bd5"></i></div><strong>${fmt(cur.dv,'currency')}</strong></div></div></article></div>`}
function renderStores(){const q=state.q.toLocaleLowerCase('pt-BR'),list=D.stores.filter(s=>state.storeRegion==='ALL'||s.region===state.storeRegion).filter(s=>{const x=storeStatus(s);return state.storeStatus==='ALL'||x.cls===state.storeStatus}).filter(s=>!q||`${s.code} ${s.name} ${s.region}`.toLocaleLowerCase('pt-BR').includes(q));$('view-stores').innerHTML=hero('Visão por Lojas','Cards compactos com semáforo e indicadores do período selecionado.')+`<div class="toolbar"><input id="store-q" placeholder="Buscar loja, código ou regional..." value="${safe(state.q)}"><select id="store-region"><option value="ALL">Todas as regionais</option>${D.regions.map(r=>`<option value="${safe(r)}" ${state.storeRegion===r?'selected':''}>${safe(r)}</option>`).join('')}</select><select id="store-status"><option value="ALL">Todos os status</option><option value="good" ${state.storeStatus==='good'?'selected':''}>Saudálvel</option><option value="warn" ${state.storeStatus==='warn'?'selected':''}>Atenção</option><option value="bad" ${state.storeStatus==='bad'?'selected':''}>Crítico</option></select><button class="btn" id="store-clear">Limpar</button><span class="count">${list.length} lojas</span></div><div class="store-grid">${list.map(s=>{const r=get(s.id),st=storeStatus(s),p=get(s.id,prevMonth(state.month)),att=r?.labels!=null?r.labels/T*100:null;return`<article class="store-card"><div class="card-head"><div><div class="code">${s.code}</div><div class="name">${safe(s.name)}</div><div class="sub">${safe(s.region)}</div></div><span class="semaphore ${st.cls} has-tip" data-tip="${attr(st.reasons.join(' '))}"><i></i>${st.label}</span></div><div class="dense"><div><span>Atingimento</span><strong>${fmt(att,'percent')}</strong></div><div><span>Etiquetas</span><strong>${fmt(r?.labels)}</strong>${trend(r?.labels,p?.labels,'up','Etiquetas auditadas')}</div><div><span>Div./mil</span><strong>${fmt(r/.labels?(r.divergences||0)/r.labels*1000:null,'decimal')}</strong></div><div><span>Sem preço/mil</span><strong>${fmt(r/.labels?(r.noPrice||0)/r.labels*1000:null,'decimal')}</strong></div></div><div class="card-foot"><button class="btn open-store" data-id="${s.id}">Abrir</button><button class="btn open-scenario" data-kind="store" data-id="${s.id}">Cenário</button><button class="btn rec-store" data-id="${s.id}">Recomendação</button></div></article>`}).join('')}</div>`}
function regionStatus(r){const ss=D.stores.filter(s=>s.region===r),stats=ss.map(s=>storeStatus(s)),bad=stats.filter(x=>x.cls==='bad').length,warn=stats.filter(x=>x.cls==='warn').length;return bad?{cls:'bad',label:'Crítico',reasons:[`${bad} loja(s) críticas na competência.`]}:warn?{cls:'warn',label:'Atenção',reasons:[`${warn} loja(s) em atenção na competência.`]}:{cls:'good',label:'Saudálvel',reasons:['Sem lojas críticas na competência.']}}
function renderRegions(){const regs=D.regions.map(r=>[r,summary(state.month,'region',r),regionStatus(r)]).sort((a,b)=>(b[1].att||0)-(a[1].att||0));$('view-regions').innerHTML=hero('Visão por Regionais','Regionais compactas com semáforo, cobertura e descontos.')+`<div class="region-grid">${regs.map(([r,s,st])=>{const p=monthSummary(prevMonth(state.month),'region',r),ds=p?.att,d=pct(s.att,ds);return`<article class="region-card" style="--region:${regionColors[r]||'#668bd5'}"><div class="region-accent"></div><div class="card-head"><div><div class="code">${safe(r)}</div><div class="sub">${s.ss.length} lojas</div></div><span class="semaphore ${st.cls} has-tip" data-tip="${attr(st.reasons.join(' '))}"><i></i>${st.label}</span></div><div class="dense"><div><span>Atingimento</span><strong>${fmt(s.att,'percent')}</strong>${d==null?'':trend(s.att,ds,'up','Atingimento','percent')}</div><div><span>Lojas na meta</span><strong>${s.hit}/${s.op.length}</strong></div><div><span>Div./mil</span><strong>${fmt(s.dr,'decimal')}</strong></div><div><span>Sem preço/mil</span><strong>${fmt(s.npr,'decimal')}</strong></div></div><div class="card-foot"><button class="btn open-region" data-id="${safe(r)}">Abrir</button><button class="btn open-scenario" data-kind="region" data-id="${safe(r)}">Cenário</button><button class="btn rec-region" data-id="${safe(r)}">Recomendação</button></div></article>`}).join(')}</div>`}
function pointValue(m,k){const v=scopeStores().map(s=>get(s.id,m)?.[k]).filter(x=>x!=null);return v.length?sum(v):null}
function lineChart(points,color,type,uid='chart'){const v=points.filter(p=>p.value!=null);if(v.length<2)return'<div style="padding:20px;color:#8092a8">Histórico insuficiente.</div>';const W=1120,H=360,L=80,R=28,T3=22,B=54, min0=Math.min(...v.map(p=>p.v)),max0=Math.max(...v.map(p=>p.v)),range=Math.max(1,max0-min0),lo=Math.max(0,min0-range*.08),hi=max0+range*.12,X=i=>L+i/(v.length-1)*(W-L-R),Y=n=>T3+(1-(n-lo)/(hi-lo))*(H-T3-B),poly=v.map((p,i)=>`${X(ii},${Y(p.v)}`).join(' '),ticks=[0,1,2,3,4].map(i=>lo+(hi-lo)*i/4),step=Math.max(1,Math.ceil(v.length/8)),clip=`clip-${uid.replace(/[^a-zA-Z0-9_-]/g,'')}-${Math.random().toString(36).slice(2,6)}`;return`<svg class="chart-svg" viewBox="0 0 ${W} ${H}" preserveAspectRatio="xMidYMid meet"><defs><clipPath id="${clip}"><rect x="${L}" y="${T3}" width="${W-L-R}" height="${H-T3-B}"/></clipPath><linearGradient id="grad-${clip}" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stop-color="${color}" stop-opacity=".18"/><stop offset="100%" stop-color="${color}" stop-opacity="0"/></linearGradient></defs><g class="chart-grid">${ticks.map(t=>`<line x1="${L}" x2="${W-R}" y1="${Y(t)}" y2="${Y(t)}"/>`).join('')}</g><g class="chart-axis">${ticks.map(t=>`<text x="${L-12}" y="${Y(t)+4}" text-anchor="end">${fmt(t,type==='currency'?'integer':type)}</text>`).join('')}${v.map((p,i)=>i%step===0||i===v.length-1?`<text x="${X(i)}" y="${H-20}" text-anchor="middle">${p.m.slice(5)}/${p.m.slice(2,4)}</text>`:'').join('')}</g><g clip-path="url(#${clip})"><polygon class="chart-area" style="--fill-color:url(#grad-${clip})" points="${L},${H-B} ${poly} ${W-R},${H-B}"/><polyline class="chart-line" style="--line-color:${color}" points="${poly}"/>${v.map((p,i)=>`<circle class="chart-dot ${i===v.length-1?'latest':'g}" style="--line-color:${color}" cx="${X(i)}" cy="${Y(p.v)}" r="${i===v.length-1?5.4:4}"><title>${mt(p.m)}: ${fmt(p.v,type)}</title></circle>`).join('')}</g></svg>`}
function columnChart(points,color,type){const v=points.filter(p=>p.v!=null).slice(-12);if(!v.length)return'—';const max=Math.max(1,...v.map(p=>Math.abs(p.v)));return`<div class="columns">${v.map(p=>`<div class="col-item has-tip" data-tip="${attr(`${mt(p.m)}: ${fmt(p.v,type)}`)}"><i style="height:${Math.max(3,Math.abs(p.v)/max*100)}%;--col:${color}"></i><span>${p.m.slice(5)}</span></div>`).join(')}</div>`}
function deltaChart(points){const v=points.filter(p=>p.v!=null),d=[];for(let i=1;i<v.length;i++){const x=pct(v[i].v,v[i-1].v);if(x!=null)d.push({m:v[i].m,v:x})}const show=d.slice(-12),max=Math.max(1,...show.map(x=>Math.abs(x.v)));return`<div class="delta-bars">${show.map(x=>`<div class="delta-row"><span>${x.m.slice(5)}</span><div class="track"><i style="width:${Math.ax(2,Math.abs(x.v)/max*100)}%;--bar:${x.v<=0?'#70bd88':'#dd6b7f'}"></i></div><strong class="${x.v<=0?'good':'bad'}">${x.v>0?'+'''}${fmt(x.v,'percent')}</strong></div>`).join(')}</div>`}
function renderEvolution(){const met=metrics[state.metric],points=D.months.filter(m=>m<=state.month).map(m=>({m,v:pointValue(m,state.metric)})).filter(p=>p.v!=null),first=points[0],last=points.at(-1),max=points.length?points.reduce((a,b)=>b.v>a.v?b:a):null,avg6=avg(points.slice(-6).map(p=>p.v));$('view-evolution').innerHTML=hero('Evolução · '+currentName(),'Histórico consolidado ate a competência selecionada.')+`<article class="panel chart-panel"><div class="panel-head"><div><h3>${met.label}</h3><p>Linha = trajetória histórica. O gráfico é confinado à área de plotagem e cada ponto possui marcador. Passe o mouse para consultar.</p></div><div class="chart-toolbar">${Object.entries(metrics).map(([k,x])=>`<button class="metric-btn ${state.metric===k?'active':''}" data-metric="${k}">${x.label}</button>`).join('')}</div></div><div class="chart-box">${lineChart(points,met.color,met.type,'evolution')}</div><div class="chart-summary"><div><span>Primeiro registro</span><strong>${first?mt(first.m)+' · '+fmt(first.v,met.type):—�}</strong></div><div><span>Último registro</span><strong>${last?mt(last.m)+' · '+fmt(last.v,met.type):—�}</strong></div><div><span>Maior valor</span><strong>${max?fmt(max.v,met.type)+' · '+mt(max.m):—}</strong></div><div><span>Média últimos 6 meses</span><strong>${fmt(avg6,met.type)}</strong></div></div></article><div class="secondary-charts"><article class="panel mini-chart"><h3>Últimos 12 meses</h3><p>Leitura por colunas com mesmos dados do gráfico principal.</p>${columnChart(points,met.color,met.type)}</article><article class="panel mini-chart"><h3>Variação mês a mês</h3><p>Comparação com a competência imediatamente anterior.</p>${deltaChart(points)}</article></div>`}
function directRecs(){const out=[],bench=networkBench();if(state.scope==='store'){const s=stores.get(+state.id),r=get(s+?.id),st=s?storeStatus(s):null;if(r&&st&&st.cls==='bad'){st.reasons.slice(0,2).forEach(reason=>out.push({cls:'bad',title:reason,action:r.labels<T?`Completar ${fmt(T-r.labels)} etiquetas adh���ڊ��r'%��njT^�+��z�(��,v�^��hq��j�h�)ڞ׬z�����r&�j{^��)����������(�w���r&�h�#j����f��Z�騞�^�ǜ�{-r����)y+h���[^�