(()=>{"use strict";
const A=window.AP,{D,T,$,st,colors,metrics,safe,attr,fmt,mt,sum,avg,get,pct,prevMonth,yearAgo,scopeStores,summary,currentName,clsDelta,deltaHtml,networkDiscountBench,storeScore,scopeSignal,signalClass,hero,priorityStores,activeMonths,trendReason}=A;
function dashboard(){const s=summary(),pm=summary(prevMonth(st.month),st.type,st.id),prior=priorityStores(s.ss,5),health=scopeSignal(s),regs=D.regions.map(r=>[r,summary(st.month,"region",r)]).sort((a,b)=>(b[1].att||0)-(a[1].att||0)),divDelta=pct(s.dr,pm.dr),npDelta=pct(s.npr,pm.npr),dvDelta=pct(s.dv,pm.dv),attDelta=pct(s.att,pm.att);$("view-dashboard").innerHTML=hero(currentName(),s)+`<section class="dashboard-fast-grid"><article class="panel gauge-panel"><div class="panel-head"><div><h3>Cenário em 10 segundos</h3><p>${safe(mt(st.month))} comparado a ${safe(mt(prevMonth(st.month)))}.</p></div><span class="semaphore has-tip ${health.cls}" data-tip="${attr(health.tip)}"><i></i>${health.label}</span></div><div class="fast-gauge-wrap"><div class="ring enterprise-ring ${signalClass(s.att)}" style="--p:${Math.min(100,s.att||0)}"><div class="ring-inner"><strong>${fmt(s.att,"percent")}</strong><span>atingimento</span></div></div><div class="gauge-copy"><strong>${s.hit}/${s.op.length} lojas na meta</strong><span>${fmt(s.labels)} etiquetas auditadas</span><div class="reference-line"><span>Meta do escopo</span><strong>${fmt(s.target)}</strong></div></div></div></article><div class="snapshot-grid"><article class="snapshot-card"><div><span>Divergências / mil</span><strong>${fmt(s.dr,"decimal")}</strong></div>${deltaHtml(divDelta,"down",trendReason(divDelta,"down","Divergências por mil",s.dr,pm.dr,"decimal"))}<div class="snapshot-track danger"><i style="width:${Math.min(100,(s.dr||0)*7)}%"></i></div><small>${fmt(s.div)} ocorrências no período</small></article><article class="snapshot-card"><div><span>Sem preço / mil</span><strong>${fmt(s.npr,"decimal")}</strong></div>${deltaHtml(npDelta,"down",trendReason(npDelta,"down","Produtos sem preço por mil",s.npr,pm.npr,"decimal"))}<div class="snapshot-track warning"><i style="width:${Math.min(100,(s.npr||0)*1.5)}%"></i></div><small>${fmt(s.np)} produtos no período</small></article><article class="snapshot-card"><div><span>Cobertura</span><strong>${fmt(s.hitRate,"percent")}</strong></div>${deltaHtml(attDelta,"up",trendReason(attDelta,"up","Cobertura de lojas na meta",s.hitRate,pm.hitRate,"percent"))}<div class="snapshot-track"><i style="width:${Math.min(100,s.hitRate||0)}%"></i></div><small>${s.hit} de ${s.op.length} lojas atingiram 6.000</small></article><article class="snapshot-card"><div><span>Valor de descontos</span><strong>${fmt(s.dv,"currency")}</strong></div>${deltaHtml(dvDelta,"down",trendReason(dvDelta,"down","Valor de descontos",s.dv,pm.dv,"currency"))}<div class="snapshot-track neutral-track"><i style="width:${Math.min(100,Math.abs(dvDelta||0)*2)}%"></i></div><small>${fmt(s.dc)} descontos registrados</small></article></div></section><div class="grid-main executive-grid"><article class="panel"><div class="panel-head"><div><h3>Prioridades de atenção</h3><p>Critérios: meta de etiquetas e descontos frente à média da rede.</p></div><button class="ghost-btn small jump-recommendations" type="button">Ver recomendações</button></div><div class="priority-list">${prior.map(x=>`<div class="priority-item ${x.score.cls}"><div><strong>${x.s.code} · ${safe(x.s.name)}</strong><span>${safe(x.s.region)} • ${x.score.label}</span></div><div class="priority-metrics"><span class="micro-pill ${signalClass(x.score.att)}">${fmt(x.score.att,"percent")} meta</span><span class="micro-pill ${x.score.cls} has-tip" data-tip="${attr(x.score.tip)}">${x.score.label}</span></div></div>`).join("")||'<div class="empty">Sem prioridades críticas no escopo.</div>'}</div></article><article class="panel"><div class="panel-head"><div><h3>Benchmark por regional</h3><p>Atingimento atual por regional.</p></div><button class="ghost-btn small jump-comparison" type="button">Comparar</button></div><div class="regional-bars">${regs.map(([r,x])=>`<button class="regional-bar-row open-region" data-id="${safe(r)}" type="button"><div><strong>${safe(r)}</strong><span>${x.hit}/${x.op.length} na meta</span></div><div class="regional-bar"><i style="width:${Math.min(100,x.att||0)}%;--bar-color:${colors[r]||"#668bd5"}"></i></div><strong>${fmt(x.att,"percent")}</strong></button>`).join("")}</div></article></div>`}
function storesView(){const q=st.q.toLocaleLowerCase("pt-BR"),list=D.stores.filter(s=>st.region==="ALL"||s.region===st.region).filter(s=>{const score=storeScore(s);return st.status==="GOOD"?score.cls==="good":st.status==="WARN"?score.cls==="warn":st.status==="BAD"?score.cls==="bad":st.status==="NODATA"?score.cls==="neutral":true}).filter(s=>!q||(`${s.code} ${s.name} ${s.region}`).toLocaleLowerCase("pt-BR").includes(q));$("view-stores").innerHTML=hero("Visão por Lojas",summary())+`<div class="toolbar premium-toolbar"><div class="toolbar-left wide"><input id="search" class="search" placeholder="Buscar loja, código ou regional..." value="${safe(st.q)}"><select id="rf" class="filter-chip"><option value="ALL">Todas as regionais</option>${D.regions.map(r=>`<option value="${safe(r)}" ${st.region===r?"selected":""}>${safe(r)}</option>`).join("")}</select><select id="sf" class="filter-chip"><option value="ALL">Todos os status</option><option value="GOOD" ${st.status==="GOOD"?"selected":""}>Saudável</option><option value="WARN" ${st.status==="WARN"?"selected":""}>Atenção</option><option value="BAD" ${st.status==="BAD"?"selected":""}>Crítico</option><option value="NODATA" ${st.status==="NODATA"?"selected":""}>Sem base</option></select><button id="reset-store-filters" class="ghost-btn small" type="button">Limpar</button></div><span class="data-meta">${list.length} lojas</span></div><div class="store-grid refined-store-grid">${list.map(s=>{const r=get(s.id),score=storeScore(s),pm=get(s.id,prevMonth(st.month)),d=pct(r?.labels,pm?.labels);return`<article class="store-card premium-store-card dense-card"><div class="store-head compact"><div><div class="store-code">${s.code}</div><div class="store-name">${safe(s.name)}</div><div class="region-tag">${safe(s.region)}</div></div><span class="badge has-tip ${score.cls}" data-tip="${attr(score.tip)}">${score.label}</span></div><div class="dense-metrics"><div><span>Atingimento</span><strong class="${signalClass(score.att)}">${fmt(score.att,"percent")}</strong></div><div><span>Etiquetas</span><strong>${fmt(r?.labels)}</strong><small class="trend has-tip ${clsDelta(d,"up")}" data-tip="${attr(trendReason(d,"up","Etiquetas auditadas",r?.labels,pm?.labels,"integer"))}">${d==null?"—":`${d>0?"▲":"▼"} ${fmt(Math.abs(d),"percent")}`}</small></div><div><span>Div./mil</span><strong>${fmt(score.dr,"decimal")}</strong></div><div><span>Sem preço/mil</span><strong>${fmt(score.npr,"decimal")}</strong></div></div><div class="dense-footer"><span>Descontos: <strong>${fmt(r?.discountValue,"currency")}</strong> • ${fmt(r?.discountCount)} ocorr.</span></div><div class="store-card-actions compact-actions triple-actions"><button class="card-action open-store" data-id="${s.id}" type="button">Abrir</button><button class="card-action secondary open-scenario" data-kind="store" data-id="${s.id}" type="button">Cenário</button><button class="card-action secondary store-recommendations" data-id="${s.id}" type="button">Recomendação</button></div></article>`}).join("")||'<div class="empty">Nenhuma loja encontrada.</div>'}</div>`}
function regionsView(){const netBench=networkDiscountBench(st.month);$("view-regions").innerHTML=hero("Visão por Regionais",summary())+`<div class="region-grid refined-region-grid">${D.regions.map(r=>[r,summary(st.month,"region",r)]).sort((a,b)=>(b[1].att||0)-(a[1].att||0)).map(([r,s])=>{const pm=summary(prevMonth(st.month),"region",r),d=pct(s.att,pm.att),countPer=s.rr.length?s.dc/s.rr.length:null,valuePer=s.rr.length?s.dv/s.rr.length:null,reasons=[];if((s.att||0)<100)reasons.push(`Atingimento abaixo da meta: ${fmt(s.att,"percent")}.`);if(countPer!=null&&netBench.count!=null&&countPer>netBench.count)reasons.push(`Qtd. média de descontos/loja acima da rede: ${fmt(countPer,"decimal")} vs ${fmt(netBench.count,"decimal")}.`);if(valuePer!=null&&netBench.value!=null&&valuePer>netBench.value)reasons.push(`Valor médio de descontos/loja acima da rede: ${fmt(valuePer,"currency")} vs ${fmt(netBench.value,"currency")}.`);const q=reasons.length?"bad":((countPer||0)>=(netBench.count||0)*.8||(valuePer||0)>=(netBench.value||0)*.8)?"warn":"good",label=q==="good"?"Saudável":q==="warn"?"Atenção":"Crítico",tip=`${label} em ${mt(st.month)}. ${reasons.length?reasons.join(" "):q==="warn"?"Meta atingida, mas descontos se aproximam da média da rede.":"Meta atingida e descontos abaixo da faixa de atenção."}`;return`<article class="region-card premium-region-card dense-card" style="--region-color:${colors[r]}"><div class="region-accent"></div><div class="region-head compact"><div><div class="region-name">${safe(r)}</div><div class="region-tag">${s.ss.length} lojas</div></div><span class="badge has-tip ${q}" data-tip="${attr(tip)}">${label}</span></div><div class="dense-metrics"><div><span>Atingimento</span><strong>${fmt(s.att,"percent")}</strong><small class="trend has-tip ${clsDelta(d,"up")}" data-tip="${attr(trendReason(d,"up","Atingimento regional",s.att,pm.att,"percent"))}">${d==null?"—":`${d>0?"▲":"▼"} ${fmt(Math.abs(d),"percent")}`}</small></div><div><span>Lojas na meta</span><strong>${s.hit}/${s.op.length}</strong></div><div><span>Div./mil</span><strong>${fmt(s.dr,"decimal")}</strong></div><div><span>Sem preço/mil</span><strong>${fmt(s.npr,"decimal")}</strong></div></div><div class="dense-footer"><span>${fmt(s.labels)} etiquetas • ${fmt(s.dv,"currency")} descontos</span></div><div class="store-card-actions compact-actions triple-actions"><button class="card-action open-region" data-id="${safe(r)}" type="button">Abrir</button><button class="card-action secondary open-scenario" data-kind="region" data-id="${safe(r)}" type="button">Cenário</button><button class="card-action secondary region-recommendations" data-id="${safe(r)}" type="button">Recomendação</button></div></article>`}).join("")}</div>`}
function point(m,k){const vals=scopeStores().map(s=>get(s.id,m)?.[k]).filter(x=>x!=null);return vals.length?sum(vals):null}
function lineChart(pts,c,t){const v=pts.filter(x=>x.v!=null);if(v.length<2)return'<div class="chart-empty">Histórico insuficiente neste escopo.</div>';const W=1120,H=360,L=76,R=38,Tp=28,B=56;let mi=Math.min(...v.map(x=>x.v)),ma=Math.max(...v.map(x=>x.v));if(mi===ma){mi*=.9;ma=ma*1.1||1}const bf=(ma-mi)*.08;mi=Math.max(0,mi-bf);ma+=bf;const X=i=>L+i/(v.length-1)*(W-L-R),Y=x=>Tp+(1-(x-mi)/(ma-mi))*(H-Tp-B),ps=v.map((p,i)=>[X(i),Y(p.v),p]),poly=ps.map(p=>p[0]+","+p[1]).join(" "),ticks=[0,1,2,3,4].map(i=>ma-(ma-mi)*i/4),step=Math.max(1,Math.ceil(v.length/8));return`<svg class="chart-svg polished-line" viewBox="0 0 ${W} ${H}" preserveAspectRatio="xMidYMid meet"><defs><linearGradient id="ag13" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stop-color="${c}" stop-opacity=".16"/><stop offset="100%" stop-color="${c}" stop-opacity="0"/></linearGradient></defs><g class="chart-grid">${ticks.map(z=>`<line x1="${L}" x2="${W-R}" y1="${Y(z)}" y2="${Y(z)}"/>`).join("")}</g><polygon points="${L},${H-B} ${poly} ${W-R},${H-B}" fill="url(#ag13)"/><polyline class="chart-line" style="--chart-color:${c}" points="${poly}"/>${ps.map((p,i)=>`<circle class="chart-dot ${i===ps.length-1?"latest":""}" style="--chart-color:${c}" cx="${p[0]}" cy="${p[1]}" r="${i===ps.length-1?5.5:4}"><title>${mt(p[2].m)}: ${fmt(p[2].v,t)}</title></circle>`).join("")}<g class="chart-axis">${ticks.map(z=>`<text x="${L-14}" y="${Y(z)+4}" text-anchor="end">${fmt(z,t==="currency"?"integer":t)}</text>`).join("")}${ps.map((p,i)=>i%step===0||i===ps.length-1?`<text x="${p[0]}" y="${H-22}" text-anchor="middle">${p[2].m.slice(5)}/${p[2].m.slice(2,4)}</text>`:"").join("")}</g></svg>`}
function columnChart(pts,c,t){const v=pts.filter(x=>x.v!=null).slice(-12);if(!v.length)return'<div class="chart-empty">Sem dados.</div>';const max=Math.max(...v.map(x=>x.v),1);return`<div class="column-chart">${v.map(x=>`<div class="column-item has-tip" data-tip="${attr(`${mt(x.m)}: ${fmt(x.v,t)}`)}"><div class="column-track"><i style="height:${Math.max(4,x.v/max*100)}%;--col:${c}"></i></div><span>${x.m.slice(5)}/${x.m.slice(2,4)}</span></div>`).join("")}</div>`}
function variationChart(pts,direction){const v=pts.filter(x=>x.v!=null).slice(-13),rows=[];for(let i=1;i<v.length;i++){const d=pct(v[i].v,v[i-1].v);rows.push({m:v[i].m,d,prev:v[i-1].v,cur:v[i].v})}const max=Math.max(1,...rows.map(x=>Math.abs(x.d||0)));return`<div class="variance-chart">${rows.map(x=>{const favorable=clsDelta(x.d,direction),w=Math.min(48,Math.abs(x.d||0)/max*48),left=(x.d||0)<0?50-w:50;return`<div class="variance-row has-tip" data-tip="${attr(`${mt(x.m)}: ${x.d==null?"sem comparação":`${x.d>0?"+":""}${fmt(x.d,"percent")}`}`)}"><span>${x.m.slice(5)}/${x.m.slice(2,4)}</span><div class="variance-track"><i class="${favorable}" style="left:${left}%;width:${w}%"></i></div><strong class="${favorable}">${x.d==null?"—":`${x.d>0?"+":""}${fmt(x.d,"percent")}`}</strong></div>`}).join("")}</div>`}
function evolution(){const[label,c,t,direction]=metrics[st.metric],pts=activeMonths().map(m=>({m,v:point(m,st.metric)})),v=pts.filter(x=>x.v!=null),last=v.at(-1),max=v.length?v.reduce((a,b)=>b.v>a.v?b:a):null,first=v[0],recent=v.slice(-6),avgRecent=recent.length?sum(recent.map(x=>x.v))/recent.length:null;$("view-evolution").innerHTML=hero("Evolução · "+currentName(),summary(),"Histórico consolidado do indicador selecionado até a competência atual.")+`<article class="panel evolution-panel polished-evolution"><div class="panel-head evolution-head"><div><h3>${label}</h3><p>Linha = trajetória histórica. Passe o mouse nos pontos para consultar o valor mensal.</p></div><div class="metric-switch premium-switch">${Object.entries(metrics).map(([k,x])=>`<button class="metric-btn ${st.metric===k?"active":""}" data-metric="${k}" type="button">${x[0]}</button>`).join("")}</div></div><div class="chart-wrap refined-chart-wrap">${lineChart(pts,c,t)}</div><div class="chart-summary"><div><span>Primeiro registro</span><strong>${first?mt(first.m)+" · "+fmt(first.v,t):"—"}</strong></div><div><span>Último registro</span><strong>${last?mt(last.m)+" · "+fmt(last.v,t):"—"}</strong></div><div><span>Maior valor</span><strong>${max?fmt(max.v,t)+" · "+mt(max.m):"—"}</strong></div><div><span>Média últimos 6 meses</span><strong>${fmt(avgRecent,t)}</strong></div></div></article><div class="evolution-secondary-grid"><article class="panel"><div class="panel-head"><div><h3>Últimos 12 meses</h3><p>Mesmos dados em leitura por colunas para comparar magnitude mensal.</p></div><span class="pill">12M</span></div>${columnChart(pts,c,t)}</article><article class="panel"><div class="panel-head"><div><h3>Variação mês a mês</h3><p>Desvio percentual frente à competência imediatamente anterior.</p></div><span class="pill">Δ M-1</span></div>${variationChart(pts,direction)}</article></div>`}
function entityHistory(kind,id,key){const ss=kind==="store"?[A.stores.get(+id)]:D.stores.filter(s=>s.region===id);return activeMonths().map(m=>{const vals=ss.map(s=>get(s.id,m)?.[key]).filter(x=>x!=null);return{m,v:vals.length?sum(vals):null}})}
function miniLine(pts,c,t){const v=pts.filter(x=>x.v!=null).slice(-12);if(v.length<2)return'<div class="modal-empty">Histórico insuficiente.</div>';const W=520,H=150,P=14,mi=Math.min(...v.map(x=>x.v)),ma=Math.max(...v.map(x=>x.v)),span=ma-mi||1,X=i=>P+i/(v.length-1)*(W-P*2),Y=x=>P+(1-(x-mi)/span)*(H-P*2),poly=v.map((p,i)=>`${X(i)},${Y(p.v)}`).join(" ");return`<svg class="modal-spark" viewBox="0 0 ${W} ${H}"><polyline points="${poly}" style="--spark:${c}"/>${v.map((p,i)=>`<circle cx="${X(i)}" cy="${Y(p.v)}" r="3"><title>${mt(p.m)}: ${fmt(p.v,t)}</title></circle>`).join("")}</svg>`}
function comparisonBars(items,type){const max=Math.max(1,...items.map(x=>x.value||0));return`<div class="modal-bars">${items.map(x=>`<div class="modal-bar-row"><span>${safe(x.label)}</span><div><i style="width:${Math.max(2,(x.value||0)/max*100)}%"></i></div><strong>${fmt(x.value,type)}</strong></div>`).join("")}</div>`}
function openScenario(kind,id){let title,sub,cur,pm,ya,net=summary(st.month,"network",""),reg=null,labelAvg,dcAvg,dvAvg;if(kind==="store"){const s=A.stores.get(+id);if(!s)return;title=`${s.code} · ${s.name}`;sub=s.region;cur=summary(st.month,"store",String(id));pm=summary(prevMonth(st.month),"store",String(id));ya=summary(yearAgo(st.month),"store",String(id));reg=summary(st.month,"region",s.region);labelAvg=cur.labels;dcAvg=cur.dc;dvAvg=cur.dv}else{title=id;sub=`Regional • ${D.stores.filter(s=>s.region===id).length} lojas`;cur=summary(st.month,"region",id);pm=summary(prevMonth(st.month),"region",id);ya=summary(yearAgo(st.month),"region",id);labelAvg=cur.op.length?cur.labels/cur.op.length:null;dcAvg=cur.rr.length?cur.dc/cur.rr.length:null;dvAvg=cur.rr.length?cur.dv/cur.rr.length:null}const networkLabelAvg=net.op.length?net.labels/net.op.length:null,networkDcAvg=net.rr.length?net.dc/net.rr.length:null,networkDvAvg=net.rr.length?net.dv/net.rr.length:null,regionLabelAvg=reg?.op.length?reg.labels/reg.op.length:null;const root=$("app-modal-root");root.innerHTML=`<div class="modal-backdrop" data-modal-close><section class="scenario-modal" role="dialog" aria-modal="true" aria-label="Cenário ${safe(title)}"><header class="scenario-modal-head"><div><p class="eyebrow">CENÁRIO ANALÍTICO • ${safe(mt(st.month).toUpperCase())}</p><h2>${safe(title)}</h2><span>${safe(sub)}</span></div><button class="modal-close" type="button" data-modal-close aria-label="Fechar">×</button></header><div class="modal-kpi-grid"><div><span>Atingimento</span><strong>${fmt(cur.att,"percent")}</strong><small>${deltaHtml(pct(cur.att,pm.att),"up",trendReason(pct(cur.att,pm.att),"up","Atingimento",cur.att,pm.att,"percent"))}</small></div><div><span>Etiquetas</span><strong>${fmt(cur.labels)}</strong><small>Ano anterior ${fmt(ya.labels)}</small></div><div><span>Qtd. descontos</span><strong>${fmt(cur.dc)}</strong><small>Ano anterior ${fmt(ya.dc)}</small></div><div><span>Valor descontos</span><strong>${fmt(cur.dv,"currency")}</strong><small>Ano anterior ${fmt(ya.dv,"currency")}</small></div></div><div class="scenario-grid"><article class="modal-panel"><h3>Etiquetas: entidade × referências</h3>${comparisonBars([{label:kind==="store"?"Loja":"Média por loja da regional",value:labelAvg},{label:"Média da rede",value:networkLabelAvg},...(kind==="store"?[{label:"Média da regional",value:regionLabelAvg}]:[]),{label:"Mesmo mês ano anterior",value:kind==="store"?ya.labels:(ya.op.length?ya.labels/ya.op.length:null)}],"integer")}</article><article class="modal-panel"><h3>Quantidade de descontos</h3>${comparisonBars([{label:kind==="store"?"Loja":"Média por loja da regional",value:dcAvg},{label:"Média da rede",value:networkDcAvg},{label:"Mesmo mês ano anterior",value:kind==="store"?ya.dc:(ya.rr.length?ya.dc/ya.rr.length:null)}],"decimal")}</article><article class="modal-panel"><h3>Valor de descontos</h3>${comparisonBars([{label:kind==="store"?"Loja":"Média por loja da regional",value:dvAvg},{label:"Média da rede",value:networkDvAvg},{label:"Mesmo mês ano anterior",value:kind==="store"?ya.dv:(ya.rr.length?ya.dv/ya.rr.length:null)}],"currency")}</article><article class="modal-panel wide"><h3>Trajetória de etiquetas • últimos 12 registros</h3>${miniLine(entityHistory(kind,id,"labels"),"#4f9f95","integer")}</article><article class="modal-panel wide"><h3>Trajetória do valor de descontos • últimos 12 registros</h3>${miniLine(entityHistory(kind,id,"discountValue"),"#668bd5","currency")}</article></div></section></div>`;document.body.classList.add("modal-open");root.querySelectorAll("[data-modal-close]").forEach(el=>el.addEventListener("click",e=>{if(e.target===el||el.classList.contains("modal-close"))closeScenario()}))}
function closeScenario(){const root=$("app-modal-root");if(root)root.innerHTML="";document.body.classList.remove("modal-open")}
Object.assign(A.views,{dashboard,stores:storesView,regions:regionsView,evolution});A.openScenario=openScenario;A.closeScenario=closeScenario;
})();